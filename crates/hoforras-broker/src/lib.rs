//! # hoforras-broker — Thermal Market & Brokerage (DDD-01, the core domain).
//!
//! Skeleton for the workspace foundation (Prompt 0). The `BrokerAgent` MRAP loop, the pure
//! `decide()` core, `FederatedTrainer`, and the daa adapters are implemented in Prompt 7.

/// Marker proving the crate is wired into the workspace and can reach the ports layer.
pub const CONTEXT: &str = "thermal-market";
