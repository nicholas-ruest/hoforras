# Rust build artifacts
/target
**/*.rs.bk
# trybuild scratch output
**/wip/

# Claude Code / claude-flow — config, agents, commands, runtime state
.claude/
.claude-flow/
CLAUDE.md
.mcp.json

# UI design-reference template (not part of the app)
COGS_UI_Template/
